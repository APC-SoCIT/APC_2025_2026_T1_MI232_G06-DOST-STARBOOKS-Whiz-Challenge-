import 'package:flutter/material.dart';

class WhizMemoryMatch extends StatelessWidget {
  const WhizMemoryMatch({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Whiz Memory Match")),
      body: const Center(child: Text("This is the Whiz Memory Match Page")),
    );
  }
}
